<ui-display>
	<ui-section title='Free channels:'>
		<ui-bar min='0' max='{{data.max_channels}}' value='{{data.channels_left}}'>{{data.channels_left}}</ui-bar>
	</ui-section>
</ui-display>
<ui-display>
	<ui-section title='Events:'>
		<ui-bar min='0' max='{{data.max_events}}' value='{{data.events_active}}'>{{data.events_active}}</ui-bar>
	</ui-section>
</ui-display>